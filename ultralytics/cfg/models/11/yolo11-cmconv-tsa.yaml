# Ultralytics YOLO 🚀, AGPL-3.0 license
# YOLO11 object detection model with CMConv + TSA modules
# CMConv: 量子计算卷积模块 - 专为半导体晶圆缺陷检测优化
# TSA: 三重尺度自适应检测头 - 基于小目标增强、多尺度融合和自适应对齐的创新检测头

# Parameters
nc: 80 # number of classes
scales: # model compound scaling constants, i.e. 'model=yolo11-CMConv-tsa.yaml' will call yolo11-CMConv-tsa.yaml with scale 'n'
  # [depth, width, max_channels]
  n: [0.50, 0.25, 1024] # summary: 319 layers, 2624080 parameters, 2624064 gradients, 6.6 GFLOPs
  s: [0.50, 0.50, 1024] # summary: 319 layers, 9458752 parameters, 9458736 gradients, 21.7 GFLOPs
  m: [0.50, 1.00, 512] # summary: 409 layers, 20114688 parameters, 20114672 gradients, 68.5 GFLOPs
  l: [1.00, 1.00, 512] # summary: 631 layers, 25372160 parameters, 25372144 gradients, 87.6 GFLOPs
  x: [1.00, 1.50, 512] # summary: 631 layers, 56966176 parameters, 56966160 gradients, 196.0 GFLOPs

# YOLO11n backbone with CMConv
backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [64, 3, 2]] # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]] # 1-P2/4
  - [-1, 2, CMConv, [256, False, 0.25]] # 2 - 使用CMConv替换C3k2
  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8
  - [-1, 2, CMConv, [512, False, 0.25]] # 4 - 使用CMConv替换C3k2
  - [-1, 1, Conv, [512, 3, 2]] # 5-P4/16
  - [-1, 2, CMConv, [512, True]] # 6 - 使用CMConv替换C3k2
  - [-1, 1, Conv, [1024, 3, 2]] # 7-P5/32
  - [-1, 2, CMConv, [1024, True]] # 8 - 使用CMConv替换C3k2
  - [-1, 1, SPPF, [1024, 5]] # 9
  - [-1, 2, C2PSA, [1024]] # 10

# YOLO11n head with CMConv + TSA
head:
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 6], 1, Concat, [1]] # cat backbone P4
  - [-1, 2, CMConv, [512, False]] # 13 - 使用CMConv替换C3k2

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 4], 1, Concat, [1]] # cat backbone P3
  - [-1, 2, CMConv, [256, False]] # 16 (P3/8-small) - 使用CMConv替换C3k2

  - [-1, 1, Conv, [256, 3, 2]]
  - [[-1, 13], 1, Concat, [1]] # cat head P4
  - [-1, 2, CMConv, [512, False]] # 19 (P4/16-medium) - 使用CMConv替换C3k2

  - [-1, 1, Conv, [512, 3, 2]]
  - [[-1, 10], 1, Concat, [1]] # cat head P5
  - [-1, 2, CMConv, [1024, True]] # 22 (P5/32-large) - 使用CMConv替换C3k2

  - [[16, 19, 22], 1, TSADetect, [nc]] # TSA Triple Scale Adaptive Detection head

# 消融实验配置：
# 1. 使用CMConv + TSA组合，验证量子计算卷积与三重尺度自适应检测的协同效果
# 2. 不使用MSAFE模块，专注于CMConv和TSA的性能贡献
# 3. CMConv模块：融合量子计算原理的卷积模块，提供更强的特征表示能力
# 4. TSA检测头：三重尺度自适应检测头，包含：
#    - Target Enhancement：小目标检测增强
#    - Scale Fusion：多尺度特征融合
#    - Adaptive Alignment：自适应特征对齐
